[
  {
    "name": "Aborts if policy allows writing to the top-level webrun.json directory",
    "args": ["src/test.js"],
    "expect": {
      "exit_code": 1,
      "stderr": [
        {"contains": "Executable in write directory"},
        {"contains": "Executable:"}
      ]
    }
  }
]

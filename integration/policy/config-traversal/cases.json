[
  {
    "name": "Blocks path traversal attacks in storage permissions explicitly",
    "args": ["test.js"],
    "expect": {
      "exit_code": 1,
      "stderr": [
        {"contains": "SecurityError"},
        {"contains": "escapes configuration boundary"}
      ]
    }
  }
]

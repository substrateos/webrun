[
  {
    "name": "Aborts if policy allows writing to the webrun executable directory",
    "args": ["src/test.js"],
    "expect": {
      "exit_code": 1,
      "stderr": [
        {"contains": "/usr/local"}
      ]
    }
  }
]

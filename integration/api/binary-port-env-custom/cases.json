[
  {
    "name": "portEnv overrides default PORT env var name",
    "runner": "cli",
    "args": ["parent.js"],
    "timeout_ms": 10000,
    "expect": {
      "exit_code": 0,
      "stdout": [
        {"contains": "PASS:custom_port_env"}
      ]
    }
  }
]

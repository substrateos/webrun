[
  {
    "name": "Blocks path traversal attacks in storage permissions explicitly",
    "args": ["--module", "test.js"],
    "expect": {
      "exit_code": 1,
      "stderr": [
        {"contains": "[Security Fatal] Storage permissions cannot traverse outside the configuration directory."},
        {"contains": "Attempted :"}
      ]
    }
  }
]

[
  {
    "name": "Hierarchical Scope Enforcement: Rejects nested sandbox requests if explicitly narrowing ungranted bindings",
    "args": ["test.js"],
    "cwd": "child",
    "expect": {
      "exit_code": 0,
      "stdout": [{"contains": "NARROW_BINDINGS_OK"}]
    }
  }
]
